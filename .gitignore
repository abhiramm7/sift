.DS_Store

# macapp build artifacts
macapp/.build/
macapp/.swiftpm/
macapp/Sift.app/
macapp/*.dmg

# Claude Code per-user config (allowlist tweaks, machine-specific permissions)
.claude/settings.local.json
